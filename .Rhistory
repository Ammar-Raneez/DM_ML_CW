#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.Maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, Q1 - (1.5*IQR), or above, Q3 + (1.5*IQR)
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
boxplot(x)
View(x)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
number_of_rows = nrow(scaled_vehicle_data)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.Maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, Q1 - (1.5*IQR), or above, Q3 + (1.5*IQR)
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
number_of_rows = nrow(scaled_vehicle_data)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.Maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
number_of_rows = nrow(scaled_vehicle_data)
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
number_of_rows = nrow(scaled_vehicle_data)
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
number_of_rows = nrow(scaled_vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
number_of_rows = nrow(scaled_vehicle_data)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
number_of_rows = nrow(scaled_vehicle_data)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis")
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
print(outlier)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
print(quant)
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
print(iqr)
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
x <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
scaled_vehicle_data <- x
boxplot(scaled_vehicle_data)
library(ggstatsplot)
ggbetweenstats(scaled_vehicle_data, Samples, D.Circ, outlier.tagging = T)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
scaled_vehicle_data <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
boxplot(scaled_vehicle_data)
library(readxl)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data)
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
scaled_vehicle_data <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
boxplot(scaled_vehicle_data)
library(ggstatsplot)
#plot box plot again to check whether outliers have been removed
boxplot(normalized~feature, scaled_vehicle_data)
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2)
ggbetweenstats(data = scaled_vehicle_data, at = c(1, 3, 5, 7, 9, 11, 13, 15))
ggbetweenstats(data = scaled_vehicle_data, col = c("red"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("red"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("red", "blue"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("red", "lightblue"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("lighred", "lightblue"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("lightred", "lightblue"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("lightgreen", "lightblue"))
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("lightgreen", "lightblue"), ylab = "Normalized Values")
ggbetweenstats(data = scaled_vehicle_data, x = Samples, y = NormalizedValues, outlier.tagging = T)
library(readxl)
library(ggstatsplot)
#read in the data
vehicle_data <- read_excel("../../vehicles.xlsx")
#data type
class(vehicle_data)
#any missing values?
sum(is.na(vehicle_data))
#separate inputs and outputs
vehicle_data_output_features <- vehicle_data[c("Class")]
vehicle_data_input_features <- vehicle_data[c(-ncol(vehicle_data))]
#normalize the input features
scaled_vehicle_data <- apply(vehicle_data_input_features, 2, scale)
#visualize outliers of data set
boxplot(scaled_vehicle_data, las = 2, col = c("lightgreen", "lightblue"), ylab = "Normalized Values")
#Based on box plot, following columns have outliers
have_outliers <- c("Rad.Ra", "Pr.Axis.Ra", "Max.L.Ra", "Pr.Axis.Rect", "Sc.Var.Maxis", "Sc.Var.maxis", "Skew.Maxis", "Skew.maxis", "Kurt.maxis")
#loop through all the columns that have outliers
for (outlier in have_outliers) {
number_of_rows = nrow(scaled_vehicle_data)
#get quantile values
quant <- quantile(scaled_vehicle_data[1:number_of_rows, outlier], probs = c(0.25, 0.75))
#calculate IQR for the respective columns
iqr <- IQR(scaled_vehicle_data[1:number_of_rows, outlier])
#An outlier is a point below, lower quartile and above upper quartile
upper_quartile <- quant[2] + 1.5*iqr
lower_quartile <- quant[1] - 1.5*iqr
#subset the data set, such that the outliers are filtered out
scaled_vehicle_data <- subset(
scaled_vehicle_data,
scaled_vehicle_data[1:number_of_rows, outlier] > lower_quartile &
scaled_vehicle_data[1:number_of_rows, outlier] < upper_quartile
)
}
#plot box plot again to check whether outliers have been removed
boxplot(scaled_vehicle_data, las = 2, col = c("lightgreen", "lightblue"), ylab = "Normalized Values")
View(vehicle_data_input_features)
View(scaled_vehicle_data)
View(scaled_vehicle_data)
